On using Java to generate assemblycode for microcontrollers.
The coding is object-oriented in that a variable is considered an object and may contain 
one or a number of registers, each a byte wide.
The variable-object may take other variable-objects as input and it contains code to 
manipulate the contained registers. 
